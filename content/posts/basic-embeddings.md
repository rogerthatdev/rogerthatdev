+++
date = '2026-02-12T07:03:47-08:00'
draft = true
title = 'Basic Embeddings'
now_playing = "The Marias - Submarine"
tags = ['explainer', 'embeddings', 'ai']
+++

About Embedding models
- domain - makes some models better than others in specific areas
- dimensions - different models have different number of dimensions which impacts complexity and storage (a lot higher than 2)
- context window - models support different size context windows which affects how much they can accept in a search query (tokens not words)
- deterministic - model that generates must retrieve 

## Domain-specificity 
<about domain specificity > 
Some text embedding models are specifically tailored to a particular domain. 
For example, `Voyage-Law-2` from Voyage AI is trained on with lots of legal documents, so it's really good at understanding legal language. I on the other hand

## Dimensionality

I'm not as sophisticated as an actual embedding model, so unlike others, like Google's `text-embedding-004` model that outputs **768 dimensions**, I'm only going to output **2 dimensions**. 

## Deterministic mapping
The model that generated the embedding has to be used to translate it back.

e.g 2-dim origin-size vs 2-dim agilility-intelligence will yield different results

<show the pug as a vector on each model>



## Let's role play


I've always wanted to be a model, and here's my chance. I'm going to play the role of an **embedding model**. I take words and turn them into numbers so that I can compare them to one another based on meaning. 

### Latent vs explicit

I'm not a machine, 